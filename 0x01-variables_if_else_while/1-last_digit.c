#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - entry poin
*
*Return: 0 means success else -1
*/
int main(void)
{
	int n;
    if 
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	return (0);
}